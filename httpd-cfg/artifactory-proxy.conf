##########################################################
## this configuration was generated by JFrog Artifactory ##
###########################################################

<VirtualHost *:8081>

    ProxyPreserveHost On
    ProxyAddHeaders off

    ServerName 34.222.53.92
    ServerAlias *.34.222.53.92
    ServerAdmin server@admin


    ## Application specific logs
    ## ErrorLog ${APACHE_LOG_DIR}/34.222.53.92-error.log
    ## CustomLog ${APACHE_LOG_DIR}/34.222.53.92-access.log combined

    AllowEncodedSlashes On
    RewriteEngine on

    RewriteCond %{SERVER_PORT} (.*)
    RewriteRule (.*) - [E=my_server_port:%1]
    ##  NOTE: The 'REQUEST_SCHEME' Header is supported only from apache version 2.4 and above
    RewriteCond %{REQUEST_SCHEME} (.*)
    RewriteRule (.*) - [E=my_scheme:%1]

    RewriteCond %{HTTP_HOST} (.*)
    RewriteRule (.*) - [E=my_custom_host:%1]

    RewriteCond "%{REQUEST_URI}" "^/(v1|v2)/"
    RewriteCond "%{HTTP_HOST}" "^(.*)\.34.222.53.92$"
    RewriteRule "^/(v1|v2)/(.*)$" "/artifactory/api/docker/%1/$1/$2" [PT]

    RewriteRule ^(/)?$      /ui/ [R,L]
    RewriteRule ^/ui$      /ui/ [R,L]

    RequestHeader set Host %{my_custom_host}e
    RequestHeader set X-Forwarded-Port %{my_server_port}e
    ## NOTE: {my_scheme} requires a module which is supported only from apache version 2.4 and above
    RequestHeader set X-Forwarded-Proto %{my_scheme}e
    RequestHeader set X-JFrog-Override-Base-Url %{my_scheme}e://34.222.53.92:%{my_server_port}e
    Header always set X-Content-Type-Options "nosniff"
    ProxyPassReverseCookiePath / /

    ProxyRequests off
    ProxyPreserveHost on
    ProxyPass "/artifactory/" http://34.222.53.92:8081/artifactory/
    ProxyPassReverse "/artifactory/" http://34.222.53.92:8081/artifactory/
    ProxyPass "/" http://34.222.53.92:8082/ nocanon
    ProxyPassReverse "/" http://34.222.53.92:8082/
</VirtualHost>
